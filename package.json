{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.ts --minify --format=esm --bundle --outdir=src/pyannotebook_reborn/static",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"wavesurfer.js": "^7.4.2"
	},
	"devDependencies": {
		"@anywidget/types": "^0.1.4",
		"esbuild": "^0.19.2",
		"typescript": "^5.2.2"
	}
}
